rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cada usuário só pode criar/atualizar o documento de lead com seu próprio uid
    match /leads/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
